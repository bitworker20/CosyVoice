## ROCm-friendly requirements for CosyVoice inference (FastAPI/WebUI)
##
## Notes:
## - Do NOT pin/install torch/torchaudio here: use the ROCm-enabled PyTorch shipped in the base image.
## - Remove CUDA-only extras (cu121 index, onnxruntime-gpu, tensorrt*).
## - Keep onnxruntime (CPU) for ONNX frontend models; provider selection is handled in code.
##
conformer==0.3.2
diffusers==0.29.0
fastapi==0.115.6
fastapi-cli==0.0.4
gdown==5.1.0
gradio==5.4.0
grpcio==1.57.0
grpcio-tools==1.57.0
hydra-core==1.3.2
HyperPyYAML==1.2.2
# HyperPyYAML 1.2.2 is incompatible with ruamel.yaml 0.19.x (runtime error: Loader.max_depth).
ruamel.yaml>=0.18.0,<0.19.0
inflect==7.3.1
librosa==0.10.2
lightning==2.2.4
matplotlib==3.7.5
modelscope==1.20.0
networkx==3.1
numpy==1.26.4
omegaconf==2.3.0
onnx==1.16.0
onnxruntime==1.18.0; sys_platform == 'linux'
onnxruntime==1.18.0; sys_platform == 'darwin' or sys_platform == 'win32'
# NOTE:
# CosyVoice uses `whisper.tokenizer` (module name) for text tokenization.
# However, `openai-whisper` has a strict `triton<3` dependency which conflicts with ROCm PyTorch (triton>=3).
# In the ROCm Docker image, we install `openai-whisper` with `--no-deps` in the Dockerfile to avoid resolver conflicts.
# openai-whisper==20231117
protobuf==4.25
pyarrow==18.1.0
pydantic==2.7.0
pyworld==0.3.4
rich==13.7.1
soundfile==0.12.1
tensorboard==2.14.0
transformers==4.51.3
x-transformers==2.11.24
uvicorn==0.30.0
wetext==0.0.4
wget==3.2

# Tokenizer dependency (used by cosyvoice/tokenizer/tokenizer.py)
tiktoken==0.7.0

# For runtime model download to a mounted volume
# NOTE: pip package name is "huggingface-hub" (module name is "huggingface_hub").
# transformers>=4.51 requires huggingface-hub>=0.30.0
huggingface-hub>=0.30.0,<1.0.0
